import { FadeIn } from "react-native-reanimated";

const FADE_IN_DURATION = 500;
const FADE_IN_ANIMATION = FadeIn.duration(FADE_IN_DURATION);

export const HomeScreenWrapperConstants = {
  FADE_IN_ANIMATION,
};
